import { createContext } from "react";

export const StudentsContext = createContext(null);
export const StudentsDispatchContext = createContext(null);
export const FacultiesContext = createContext(null);
export const FacultiesDispatchContext = createContext(null);
